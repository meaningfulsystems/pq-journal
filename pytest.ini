[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    requires_ollama: requires a running Ollama instance
    requires_whisper: requires faster-whisper installed
    requires_hf: requires HuggingFace transformers installed
    requires_liboqs: requires liboqs Python bindings installed
