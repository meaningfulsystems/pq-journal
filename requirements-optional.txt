# Transcription — choose one or install both (faster-whisper preferred)
faster-whisper>=1.0
vosk>=0.3.45

# Text emotion classification
transformers>=4.40
torch>=2.2

# Facial emotion — DeepFace (auto-downloads ~6 MB model on first use)
deepface>=0.0.93
tf-keras>=2.16          # required by DeepFace when TensorFlow >= 2.16 is installed

# PDF export
weasyprint>=62

# Optional audio utilities
sounddevice>=0.4
